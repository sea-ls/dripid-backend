spring:
  application:
    name: @project.artifactId@

  cloud:
    config:
      server:
        git:
          uri: https://github.com/sea-ls/dripid-config.git
          username: my_temp_access_token
          password: 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
          search-paths: "{application}"

server:
  port: 8888